apiVersion: apps/v1
kind: Deployment
metadata:
  name: discovery-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      name: discovery-service
  template:
    metadata:
      labels:
        name: discovery-service
    spec:
      containers:
        - name: discovery-service
          image: ghcr.io/luanvuhlu/discovery-server:latest
---
apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  namespace: default
spec:
  type: NodePort
  selector:
    name: discovery-service
  ports:
    - port: 8080
      targetPort: 8080
#      nodePort: 30082