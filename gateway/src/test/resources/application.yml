logging:
  level:
    "[root]": INFO

server:
  port: 8080
spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
      - id: bookStoreModule
        uri: lb://book-store
        predicates:
        - Path=/book-store/**
        filters:
        - RewritePath=/book-store/(?<segment>.*), /$\{segment}
        
eureka:
  client:
    enabled: false
management:
  endpoints:
    web:
      exposure:
        include: "*"